幫我建立一個 npm 專案，並滿足以下內容：

* 執行後會從 command-line 讀入一個參數，並在 console 上顯示 "Hello, ${name}"。
* 所有原始檔都放於 /src 底下，並在 import 時使用 path alias。
* 使用 typescript 開發。
* 使用 esbuild 打包。
* 安裝 eslint，並套用 airbnb 的規定。
* 安裝 prettier，並與 eslint 整合在一起。
* 安裝 jest。
* 執行 npm run dev 時，不預先執行打包。
