

## Make this command available globally at localhost
During development, run `npm link` to create a symbolic link for temporary access.

After the development comes to an end, run `npm install -g .` 